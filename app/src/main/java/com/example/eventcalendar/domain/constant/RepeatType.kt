package com.example.eventcalendar.domain.constant

enum class RepeatType(val type: String) {
    NEVER("Never"),
    EVERYDAY("Everyday"),
    EVERY_WEEK("Every Week"),
    EVERY_MONTY("Every Month"),
    EVERY_YEAR("Every Year")
}